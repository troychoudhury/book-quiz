# Book Quiz — Frontend Environment Variables
# Vite prefixes with VITE_ and exposes to client-side code.

# API base URL (no trailing slash). Set to your Cloud Run URL in production.
VITE_API_URL=https://book-quiz-api-xxxxx-uc.a.run.app
